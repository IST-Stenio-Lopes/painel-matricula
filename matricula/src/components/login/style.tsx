import styled from 'styled-components';
import { css } from "styled-components";
import book from '../../assets/books.png';

export const RememberKey = styled.span`
    color: #B4BFC9;
    margin-left: 2%;
`
export const ImageLateralLogin = styled.img`

`
export const BackgroundImgLateralLogin = styled.div`
  flex: 1;
  background: url(${book}) no-repeat center;
  background-size: cover;
  height: 100%;
`

//url(${book})