import React from "react";

/* export function DeleteData(array: any[], index: number) {
    //console.log(array.find(x => x.id === index));
    const arrayUpdate: any[] = array;
    arrayUpdate.splice(index -1, 1);
    console.log(arrayUpdate); 
    return arrayUpdate;
} */